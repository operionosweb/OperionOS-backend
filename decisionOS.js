import { requestLegacyAI } from "./services/ai/legacyAIRequest.js";
export async function generateDecisionOS({ contract, stressTest, transition, fleetRisk, maintenance, organizationId, confirmation = false }) { const result = await requestLegacyAI({ organizationId, operation: "decision_reasoning", input: JSON.stringify({ contract, stressTest, transition, fleetRisk, maintenance }), confirmation, system: "Return structured JSON for an aviation operating decision." }); return result.result; }

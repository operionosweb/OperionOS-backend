import { requestLegacyAI } from "./services/ai/legacyAIRequest.js";
export async function generateNegotiationSimulation({ contract, organizationId, confirmation = false }) { const result = await requestLegacyAI({ organizationId, operation: "negotiation_reasoning", input: JSON.stringify(contract), confirmation, system: "Return structured JSON for aviation contract negotiation scenarios." }); return result.result; }

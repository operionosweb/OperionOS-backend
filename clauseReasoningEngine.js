import { requestLegacyAI } from "./services/ai/legacyAIRequest.js";
export async function reasonClause(clause, organizationId, confirmation = false) { const result = await requestLegacyAI({ organizationId, operation: "clause_interpretation", input: JSON.stringify(clause), confirmation, system: "Return structured JSON explaining this aviation contract clause." }); return result.result; }
export async function reasonClauses(clauses = [], organizationId, confirmation = false) { const results = []; for (const clause of clauses) results.push(await reasonClause(clause, organizationId, confirmation)); return results; }
